apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-mqtt
spec:
  type: NodePort
  selector:
    app: bullseye-mqtt
  ports:
    - name: broker
      port: 9001
      targetPort: 9001
      protocol: TCP
